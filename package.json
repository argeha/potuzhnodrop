{
  "name": "potuzhno-drop-demo",
  "version": "5.3.0",
  "private": true,
  "description": "Potuzhno Drop — transparent virtual CS2 collection game on Cloudflare Workers.",
  "type": "module",
  "engines": {
    "node": ">=22.12 <25"
  },
  "scripts": {
    "build:styles": "tailwindcss -c tailwind.config.cjs -i css/tailwind.input.css -o css/tailwind.css --minify",
    "build:assets": "node scripts/build-assets.mjs",
    "check": "node --check js/app.js && node --check src/worker.mjs && npm run build:styles && npm run build:assets && npx wrangler deploy --dry-run",
    "build": "npm run check",
    "dev": "npm run build:styles && npm run build:assets && wrangler dev"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17",
    "wrangler": "^4.135.0"
  }
}
