{
  "name": "potuzhno-drop-demo",
  "version": "3.7.2",
  "private": true,
  "description": "Potuzhno Drop — transparent parody game with virtual CS2 inventory.",
  "type": "module",
  "engines": {
    "node": ">=22.12 <25"
  },
  "scripts": {
    "check": "node --check js/app.js && node --check netlify/functions/steam-auth.mts && node --check netlify/functions/steam-inventory.mts && node --check netlify/functions/skin-catalog.mts"
  },
  "devDependencies": {
    "@netlify/functions": "^6.0.0"
  }
}
