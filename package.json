{
  "name": "potuzhno-drop-demo",
  "version": "4.1.0",
  "private": true,
  "description": "Potuzhno Drop — transparent virtual CS2 collection game on Cloudflare Workers.",
  "type": "module",
  "engines": {
    "node": ">=22.12 <25"
  },
  "scripts": {
    "check": "node --check js/app.js && node --check src/worker.mjs && npx wrangler deploy --dry-run",
    "build": "npm run check",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.135.0"
  }
}
